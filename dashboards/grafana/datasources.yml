apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://localhost:9108
    isDefault: true
    jsonData:
      httpMethod: GET
      timeInterval: 5s
    editable: true

